// Coins: [1, 2, 5]
// Amount: 11
// Answer: 3 coins (because 5 + 5 + 1 = 11)

function minCoinChange(coins, amount){
  let arr = new Array(amount)
  let numOfCoins = +Infinity;
  for(let i = 0; i < coins.length; i++){
      if(coins.includes(coins[i])){

      }
  }  
}